{
  "name": "hotel-mgnt-root",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "admin",
    "frontend"
  ],
  "scripts": {
    "build:admin": "npm --prefix admin run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:admin"
  },
  "dependencies": {
    "connect-redis": "^9.0.0",
    "express-session": "^1.19.0",
    "google-auth-library": "^10.6.2",
    "redis": "^5.12.1"
  }
}
