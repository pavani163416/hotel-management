node_modules
dist
dist-ssr
*.local

# Environment files — never commit secrets
.env
.env.local
.env.*.local
# .env.production is committed intentionally (contains only public API URL)

.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
